akka.javasdk {
  agent {
    model-provider = anthropic

    anthropic {
      # The study fixes ONE model for the comparison; set it here.
      model-name = "claude-haiku-4-5"
      api-key = ${?ANTHROPIC_API_KEY}
    }
  }
}
